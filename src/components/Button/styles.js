import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    wrapper: {
        borderRadius: 200, 
        height: 50, 
        flexDirection: 
        'row', 
        alignItems: 'center', 
        justifyContent: 'center',
        marginBottom: 32,
        borderWidth: 0.7,
    },
    text: {
        marginLeft: 20, 
        fontSize: 16,
    },
});