import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    wrapper: {
        marginBottom: 25,
    },
    input: {
        fontSize: 18,
        marginTop: 12,
        borderBottomWidth: 1,
        paddingBottom: 8,
    }
});